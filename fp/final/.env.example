# Copy to .env and fill in. Never commit .env.
# Hugging Face read token (https://huggingface.co/settings/tokens); needs access to the gated DINOv3 repos.
HF_TOKEN=
# Optional: where the dataset copy and cleaned images go (default: ./work)
# BANANA_WORK_DIR=
